{
  "name": "openai-mtls-provider",
  "version": "1.0.0",
  "description": "OpenAI-compatible provider with mTLS support for AI SDK",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^1.0.30"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "ai": "^3.0.0 || ^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
